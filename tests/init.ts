// Пустой setup-файл для vitest (jsdom-окружение не требует доп. полифиллов для текущих тестов).
// Существование файла обязательно: `vitest.config.ts` ссылается на него через `test.setupFiles`,
// но сам файл отсутствовал в репозитории (не трекается в git, не создаётся ни одним скриптом) —
// без него не запускается ни один существующий тест, включая src/lib/gramjs/helpers.test.ts.
export {};
